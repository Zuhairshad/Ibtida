<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  SCAFFOLDING — this Info.plist is written to be dropped into a real Xcode
  "Device Activity Monitor Extension" target, added by hand (see
  docs/ios-family-controls-entitlement.md). It cannot be attached to a build
  by a config plugin alone: Expo config plugins can create/modify files and
  some Xcode project settings, but creating a brand-new extension TARGET
  (with its own product type, embed-in-host-app build phase, and signing) is
  not something `@expo/config-plugins`' documented mod APIs expose — Xcode
  project target creation is done through Xcode itself.

  $(PRODUCT_MODULE_NAME) below is filled in by Xcode's build system from the
  extension target's own module name once that target exists — do not
  replace it with a literal string.
-->
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>XPC!</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.deviceactivity.monitor-extension</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).IbadahDeviceActivityMonitor</string>
	</dict>
</dict>
</plist>
